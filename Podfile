# Uncomment this line to define a global platform for your project
# platform :ios, '8.0'

target 'tedxtimer' do

       pod 'PureLayout', '~> 2.0.6'
       pod 'Underscore.m', '~> 0.2'
       pod 'ComponentKit'
       
       pod 'FLEX', :configurations => ['Debug']
       pod 'Reveal-iOS-SDK', :configurations => ['Debug']

end

target 'tedxtimerTests' do

end

